{
  "version": 2,
  "builds": [
    {
      "src": "server/server.py",
      "use": "@vercel/python",
      "config": { "maxLambdaSize": "50mb" }
    }
  ],
  "routes": [
    { "src": "/(.*)", "dest": "server/server.py" }
  ]
}